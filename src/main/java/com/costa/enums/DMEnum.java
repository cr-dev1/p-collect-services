package com.costa.enums;

public enum DMEnum {

	type,version,datevalid,username,password,sessionId,expirationDate, date, deviceId, deviceDescription, device_description, device_type, device_date, device_Id, comarch_token, access_token, refresh_token, appTokenExpiryDate,expires_in
}
